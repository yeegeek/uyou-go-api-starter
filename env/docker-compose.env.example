# Docker Compose (dev) 常用环境变量示例
#
# 用途：
# - 给 docker-compose.yml 提供端口与参数（MongoDB / Redis / RabbitMQ）
# - 建议复制到项目根目录 .env（或把内容合并进你已有的 .env）
#
# 注意：
# - 这些变量只影响「容器对宿主机暴露的端口」以及部分服务初始化参数
# - 生产环境建议使用托管服务或单独的 prod compose 文件
#

## ---------------------------
## MongoDB
## ---------------------------

# 宿主机暴露端口（host:container）
MONGODB_PORT=27017

# MongoDB 初始化数据库名（容器环境变量）
MONGODB_DATABASE=uyou_api

# 容器名称（可选）
MONGODB_CONTAINER_NAME=go_api_mongodb


## ---------------------------
## Redis
## ---------------------------

# 宿主机暴露端口（host:container）
REDIS_PORT=6379

# 容器名称（可选）
REDIS_CONTAINER_NAME=go_api_redis


## ---------------------------
## RabbitMQ
## ---------------------------

# 宿主机暴露端口（host:container）
RABBITMQ_PORT=5672
RABBITMQ_MANAGEMENT_PORT=15672

# RabbitMQ 默认账号（容器环境变量）
RABBITMQ_USER=guest
RABBITMQ_PASSWORD=guest

# 容器名称（可选）
RABBITMQ_CONTAINER_NAME=go_api_rabbitmq

