# ===========================================
# Go REST API Boilerplate - Development Configuration  
# ===========================================
#
# This file contains development-specific overrides
# 📋 For ALL configuration options see: config.yaml (base configuration)
# 🔧 Environment variables (.env) have highest priority and will override these values
#
# ===========================================

app:
  name: "UYou API Starter API (development)"
  environment: "development"
  debug: true                       # Enable debug mode in development

database:
  host: "db"
  port: 5432
  user: "postgres"
  password: "postgres"              # Default password for development
  name: "uyou_api"
  sslmode: "disable"                # SSL disabled for development convenience

jwt:
  ttlhours: 24

server:
  port: "8080"
  readtimeout: 10
  writetimeout: 10
  idletimeout: 120
  shutdowntimeout: 30
  maxheaderbytes: 1048576

logging:
  level: "debug"
