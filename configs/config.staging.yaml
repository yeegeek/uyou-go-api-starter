# ===========================================
# Go REST API Boilerplate - Staging Configuration
# ===========================================
#
# This file contains staging-specific overrides
# 📋 For ALL configuration options see: config.yaml (base configuration)  
# 🔧 Environment variables (.env) have highest priority and will override these values
#
# ===========================================

app:
  name: "UYou API Starter API (staging)"
  environment: "staging"
  debug: false                      # Debug disabled in staging

database:
  host: "db"
  port: 5432
  user: "postgres"
  password: "postgres"              # Default for staging (override with DATABASE_PASSWORD)
  name: "uyou_api"
  sslmode: "require"                # SSL required in staging

jwt:
  ttlhours: 24

server:
  port: "8080"
  readtimeout: 10
  writetimeout: 10
  idletimeout: 120
  shutdowntimeout: 30
  maxheaderbytes: 1048576

logging:
  level: "info"
